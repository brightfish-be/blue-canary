<?php
return [

    /*
    |--------------------------------------------------------------------------
    | API version
    |--------------------------------------------------------------------------
    |
    | Define the API version to be used. Assigning a version here or in
    | the `.env` assumes a routes file exists `/routes/api-{version}.php`
    |
    */

    'api_version' => env('API_VERSION', 'v1'),
];